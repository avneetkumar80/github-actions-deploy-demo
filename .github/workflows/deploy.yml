name: Deploy to Server Using GitHub Actions

on:
  push:
    branches:
      - main

jobs:
  deploy-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Print Deployment Message
        run: echo "Deploying application using GitHub Actions"